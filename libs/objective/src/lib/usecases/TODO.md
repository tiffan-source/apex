- [] Rename the name of those usecases to be more descriptive and specific to the task they perform in business logic.

Ex : We are not creating a objective, we are defining an objective, so the usecase should be called DefineObjectiveUsecase instead of CreateObjectiveUsecase.
